import React from 'react';

// const Icon = ({className}) => {
//     <i className={className}></i>
// }
const Icon = ({className }) => {
    return <i className={className} ></i>;
};
export default Icon;